{:deps {:aliases [:plugin-build]}
 :builds
 {:engine {:target :esm
           :compiler-options {:optimizations :simple}
           :output-dir "dist-release"
           :modules {:main {:entries [paradise.engine.matrix.core]
                            :exports {bootstrap paradise.engine.matrix.core/bootstrap}}}}}}
